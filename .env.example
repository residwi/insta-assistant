# Credentials are OPTIONAL. A valid saved session (data/session.json) logs you
# in silently with no prompt. You are asked for credentials only when the
# session has expired. Fill these in to make re-auth non-interactive; leave
# blank to be prompted on re-auth instead.
INSTAGRAM_USERNAME=
INSTAGRAM_PASSWORD=
